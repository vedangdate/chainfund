{
  "name": "chainfund-contracts",
  "version": "1.0.0",
  "description": "ChainFund — on-chain crowdfunding smart contracts (Hardhat)",
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "seed:local": "hardhat run scripts/seed.js --network localhost",
    "newwallet": "node scripts/new-wallet.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17",
    "dotenv": "^16.4.7"
  }
}
